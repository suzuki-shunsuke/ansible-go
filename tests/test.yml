---
- hosts: localhost
  connection: local
  roles:
  - role: ansible-go
    go_version: 1.6.1
    go_os: linux
    go_arch: amd64
  tasks:
  - command: /usr/local/go/bin/go version
    register: result
  - debug: var=result
